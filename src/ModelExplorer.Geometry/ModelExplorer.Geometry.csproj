<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>ModelExplorer.Geometry</RootNamespace>
    <!-- Parsers use Span/Unsafe reads over pooled buffers on the hot path. -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="LibTessDotNet" Version="1.1.15" />
  </ItemGroup>

</Project>
